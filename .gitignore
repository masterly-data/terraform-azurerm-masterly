# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log

# local variable files — committed exceptions: the demo deployment's non-secret tfvars
*.tfvars
!deployments/**/terraform.tfvars

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code worktrees. Each one is its own git repository, so committing the path adds
# a gitlink rather than files — and `git add -A` picks it up silently.
.claude/worktrees/
